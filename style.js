const n=10;
const array=[];

for(let i=0;i<n,i++){
    array[i]=Math.random();
}
console.log(array); 

/* permet de créer un tableau vide, dans lequel : 
on ajoute (i++) 
des chiffres aléatoires (math.random)
on affiche le résultat en console.log*/

for(let i=0;i<array.length;i++){
    const bar=document.createElement("div");
}